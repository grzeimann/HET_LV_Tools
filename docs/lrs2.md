# LRS2

The high-level LRS2 component hierarchy used by the package is:

```text
amplifier -> channel -> spectrograph -> instrument
```

Detailed channel, spectrograph, trace, and IFU-plane mappings remain explicit
inputs so they can be populated from the existing LRS2 reduction pipeline.

